const express = require("express");
const router = express.Router();
const User = require("../models/User");
const response = require("../utils/response");

router.post("/searchCategoryList", (req, res) => {

})

module.exports = router;
